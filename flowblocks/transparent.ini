[block]
name = Transparent Block
button_label = Transparent Box

[fields.body]
label = Body
type = flow

[fields.class]
label = Style
type = select
choices = over-wide, full-width, pull-left, pull-right
choice_labels = Over Wide, Full Width, Pull Left, Pull Right
default = over-wide
width = 1/2
