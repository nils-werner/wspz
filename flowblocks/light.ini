[block]
name = Light Block
button_label = Grey

[fields.text]
label = Text
type = markdown

[fields.class]
label = Style
type = select
choices = over-wide, full-width, pull-left, pull-right
choice_labels = Over Wide, Full Width, Pull Left, Pull Right
default = over-wide
