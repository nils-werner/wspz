[model]
name = Events
label = Events
hidden = yes

[children]
model = event
order_by = -date, title
slug_format = {{ (this.date|dateformat('YYYY/M/') if this.date) ~ this._id }}

[fields.title]
label = Title
type = string
size = large
